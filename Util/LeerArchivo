#include <fstream>
#include <string>

usar namespace

int main (){
    std::ifstream myfile; myfile.open("shopping_list.txt");
    the above method
}


udacity read file in c++


if(myfile.is_open()){
    myfile >> mystring;
    std::cout << mystring;
}